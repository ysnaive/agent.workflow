---
namespace: "[Namespace/Module]"
doc_type: "design_notes"
status: "draft"
source_paths:
  - "[src/path/to/file]"
related_docs:
  - "./README.md"
last_updated: "YYYY-MM-DD"
---

# Baz — 工程妥協記錄 (Design Notes)

> 本文件記錄在 `[Namespace/Module]` 模塊中，因效能、平台限制、框架行為或其他工程約束，
> 而採用**非最直觀設計**的決策。目的是防止未來的開發者在不了解原因的情況下「修正」這些有意為之的設計。

---

## DN-01：[妥協設計的標題]

### 現狀

[目前的實作方式是什麼。一句話描述「看起來奇怪的地方」。]

```text
// 示例：說明現在的實作長什麼樣
// （使用 Foo、Bar 等示例命名，避免與真實程式碼混淆）
var fooBuffer = new FooBuffer(capacity: 64);
fooBuffer.PreallocateAll(); // 為什麼一次配置而非按需？見下方說明
```

### 限制來源

> 說明這個「看起來奇怪」的設計是因為什麼原因而不得不這樣做。限制可能來自：

- **效能量測**：[例：測試顯示按需配置在高頻路徑下造成 X ms 的延遲]
- **平台/框架限制**：[例：Foo SDK 在 Y 情況下有 Z 的行為限制]
- **記憶體約束**：[例：GC 壓力在此場景下不可接受]
- **向後相容**：[例：現有使用者依賴此行為]

### 後果與監控

[這個妥協帶來的代價是什麼？在什麼條件下應重新評估？]

- **代價**：[例：記憶體預分配增加約 X KB 的常駐開銷]
- **重新評估條件**：[例：當 Foo SDK 版本升級後，應驗證此限制是否仍然存在]

> [!CAUTION] 知識點：[坑點標題]
> [給未來開發者的一句警告。什麼情況下「修改」這段程式碼會出問題。]

---

## DN-02：[下一個妥協設計標題]

> 按需新增。每個 DN 獨立編號，不刪除舊紀錄（已解除的妥協加上「已解除」標註）。

### 現狀

[...]

### 限制來源

[...]

### 後果與監控

[...]
